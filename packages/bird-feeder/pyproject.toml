[project]
name = "bird-feeder"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "albatross",
    "httpie>=3.2.4",
]

[tool.uv.sources]
albatross = { workspace = true }

[build-system]
requires = ["uv_build>=0.11.2,<0.12"]
build-backend = "uv_build"

